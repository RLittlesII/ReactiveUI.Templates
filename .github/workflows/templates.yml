# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActionsSteps (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_templates --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: templates

on:
  push:
    branches:
      - master
      - next
    tags:
      - v*
  pull_request:
    branches:
      - master
      - next

jobs:
  Build:
    strategy:
      fail-fast: false
      matrix:
        os: [macOS-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          clean: 'false'
      - name: 📲 Install Nuke Global Tool
        run: |
          dotnet tool install -g Nuke.GlobalTool
      - name: 🎁 Restore
        run: |
          nuke Restore --skip
      - name: ⚙ Build
        run: |
          nuke Build --skip
      - name: 📲 Install For Test
        run: |
          nuke InstallForTest --skip
      - name: 🚦 Test
        run: |
          nuke Test Trigger_Code_Coverage_Reports Generate_Code_Coverage_Report_Cobertura Generate_Code_Coverage_Badges Generate_Code_Coverage_Summary Generate_Code_Coverage_Report --skip
      - name: 📦 Pack
        run: |
          nuke Pack --skip
      - name: 📲 Install
        run: |
          nuke Install --skip
